import { themeQuartz } from 'ag-grid-community';

/** Shared AG Grid theme used across all grid components. */
export const agGridTheme = themeQuartz.withParams({
  borderRadius: 6,
  headerFontWeight: 500,
  fontSize: 13,
});
